#include <iostream>
#include <vector>
using namespace std;

int main() {
    int numRows = 3;
    vector<vector<int>> v(numRows); // Inicializa 'v' con 3 vectores vacíos

    v[0].push_back(1); 
    v.push_back({1,1}); 
    int n = numRows;
    for(int i = 0; i < numRows; i++){
        int res =
        for(int j = 1; j <= numRows; j++){
            
        }
        v[i].push_back(res);
    }

    return 0;
}
